# CLAUDE.md

이 파일 하나로 프로젝트 전체 맥락이 전달되도록 작성되었다.
저장소 루트에 두면 Claude Code가 자동으로 읽는다.

---

## 이 문서의 위치

**이건 명세가 아니라 참고 자료다.** 개발은 사용자와 에이전트가 같이 진행한다.

- 여기 적힌 구현 방식(기술 스택, 데이터 모델, 구현 순서, 계산식, 화면 구성)은
  **더 나은 방법이 있으면 그쪽을 택한다.** 문서에 적혀 있다는 이유로 고수할 필요 없다.
- 문서가 틀렸거나 현실에 안 맞으면 그렇다고 말하고 대안을 제시할 것.
- 작성 시점에 모르던 제약이나 더 나은 라이브러리·패턴이 있으면 반영할 것.

다만 **2번 "반드시 지킬 설계 원칙"과 1번의 문제 정의·포지셔닝은 예외**다.
이건 구현 방식이 아니라 "무엇을 만들 것인가"에 대한 결정이고,
여러 대안을 검토한 끝에 내린 것이다.
여기에 이견이 있으면 조용히 바꾸지 말고 **먼저 말하고 논의할 것.**

---

## 작업 규칙

### 말 없이 기능을 추가하지 않는다

이 문서에 없는 기능이 필요해 보이면 **먼저 제안하고 승인을 받는다.**
"있으면 좋을 것 같아서" 붙인 기능이 하루 스코프를 잡아먹는 주범이다.
버그 수정이나 명백히 빠진 연결 작업은 예외지만, 새 화면·새 지표·새 상태는 반드시 물어본다.

### 세션이 길어지면 `/compact` 를 요청한다

대화가 길어져 맥락이 흐려지기 시작하면,
작업을 이어가기 전에 **"여기서 `/compact` 한 번 하는 게 좋겠다"고 사용자에게 말한다.**
알아서 판단하지 말고 적당한 구획(Phase 하나가 끝난 시점 등)에서 제안한다.

### 코드가 길어지면 검증을 요청한다

한 파일이나 한 함수가 길어지고 있다고 느껴지면,
**"코드가 필요 이상으로 길어진 건 아닌지 검증해달라고 지시해달라"고 사용자에게 요청한다.**

스스로 판단해서 리팩터링하지 말고, 사용자가 확인하고 지시하게 한다.
하루짜리 프로젝트에서는 "깔끔하게 정리하기"가 시간을 가장 많이 잡아먹는다.

---

## 0. 한 줄 요약

**장바구니에 잔뜩 담아두고 뭘 살지 못 정하는 사람을 위해, 목록을 정리해주는 웹 서비스.**

1인 개발 해커톤(BYPP) 출품작. 하루 안에 기획·개발·배포·실사용자 공개까지 끝낸다.

---

## 1. 대상과 문제

### 대상

```
돈은 모으고 싶은데 사고 싶은 것도 많아서
결국 아무것도 못 정하는 사람
```

### 문제

기존 위시리스트·가계부 도구는 전부 **"너무 많이 산다"**를 전제로 만들어져 있다.
과소비를 막고, 지출을 줄이고, 충동구매에 쿨다운을 건다.

실제로 겪는 문제는 반대쪽이다.

```
장바구니에 잔뜩 쌓임
   ↓
뭐가 진짜 필요한 건지 못 고름
   ↓
결정을 무기한 미룸  →  아무것도 안 삼
   ↓
정작 필요했던 걸 못 사서 아쉬움
```

**과소비가 아니라 결정 마비**이고, 결과는 낭비가 아니라 **필요한 걸 못 사는 것**이다.

### 포지셔닝

| | 기존 도구 | 이 서비스 |
|---|---|---|
| 전제 | 너무 많이 산다 | 못 고른다 |
| 목적 | 안 사게 막기 | 결정을 끝내주기 |
| 성공 기준 | 지출 감소 | 미뤄둔 결정이 정리됨 |
| "사도 됨" 판정 | 실패 | **성공** |

---

## 2. 반드시 지킬 설계 원칙

프로젝트 전반의 판단 기준이다. 구현 중 선택지가 생기면 이 원칙으로 결정한다.

### ① 결정권은 사용자에게 있다

원래 문제가 "결정을 못 함"이므로, AI가 전부 판정해버리면 결정 자체를 뺏는 셈이 된다.
**AI는 사용자가 막혔을 때만 등장한다.**

부수 효과로 API 호출이 줄어 비용도 낮아진다.

### ② 절약을 강요하지 않는다

예산 제약이 있는 이상 절약 요소를 완전히 떼어낼 수는 없다.
기회비용 표시, 예산 초과 안내는 그대로 두되 두 가지를 지킨다.

- **"사도 됨"이 실패처럼 보이지 않게 한다.**
  예산 안에서 산 것은 정상 소비다. 경고색·후퇴 연출 금지.
- **전체 지출을 줄이라고 말하지 않는다.**
  예산 안에서 순위를 매기는 것이지, 예산 자체를 줄이라고 하지 않는다.

### ③ 메시지는 해결책 형태로 쓴다

```
✕  "이거 사면 목표가 3개월 밀려요"        ← 죄책감
△  "3개월 밀려요"                         ← 손실 프레임
○  "2개 빼면 이번 달 안에 들어와요"        ← 해결책
```

### ④ LLM에게 순위를 직접 물어보지 않는다

LLM은 **항목별 파라미터만 추정**하고, 순위 계산은 코드가 한다.
LLM이 순위를 매기면 같은 입력에 매번 다른 결과가 나와 재현이 안 된다.

### ⑤ 목록이 줄어드는 것이 핵심 가치다

정리 도구를 표방하는데 목록이 안 줄면 앞뒤가 맞지 않는다.
"빼기 / 내리기" 동작은 부가 기능이 아니라 완결 장치다.

### ⑥ 재미는 장식이 아니라 기능이다

이 서비스는 **게임처럼 만드는 것이 목표**다. 쓸모 있어서 쓰는 게 아니라
**쓰고 싶어서 쓰게** 만드는 것이 핵심이다.

정리라는 행위 자체가 원래 귀찮은 일인데, 이걸 재미있게 만들지 못하면
"유용하지만 안 쓰는 도구"가 된다. 실제로 기존 가계부·위시리스트 앱 다수가 그렇다.

따라서 **UI와 연출은 마지막에 붙이는 장식이 아니라 제품의 일부**다.
연출이 붙는 지점은 8번, 비주얼 명세는 별도 `DESIGN.md` 참고.

---

## 3. 서비스 구조

```
┌─────────────────┐        ┌─────────────────┐
│   장바구니       │  ───▶  │  진짜 살 물건    │
│  (모든 항목)     │        │  (이번 달 결정)  │
└─────────────────┘        └─────────────────┘
        │                          │
        ↓ 빼기                     ↓ 내리기
     (안 살 것)                 (실제로 삼)
```

- 항목을 등록하면 **무조건 장바구니부터** 시작
- 사용자가 직접 "진짜 살 물건"으로 옮길 수 있음
- 고민되면 여러 개 선택해서 AI에게 물어봄
- **빼기** = 안 살 것으로 확정, 목록에서 제거
- **내리기** = 실제로 샀음, 목록에서 제거 (변기물 내리듯)

---

## 4. 사용자 흐름

```
① 목표 선택       은퇴 / 1억 모으기 / 내집마련 / 노트북 사기 …
② 예산 설정       안다 → 직접 입력
                  모른다 → 수입·소비 입력 → 몬테카를로 → 산출
③ 항목 등록       링크 붙여넣기 → OG 파싱 → 이유 객관식 선택
④ 분류            직접 옮기기 or "AI야 골라줘"
⑤ 예산 점검       초과 시 조정 제안 / 미달 시 목표 진척 표시
⑥ 정리            빼기 / 내리기
```

**②를 선택 경로로 두는 이유**: 수입·소비 입력을 앞에 두면 항목 등록 전에 이탈한다.
"수입 알려주세요"가 아니라 "내 적정 예산이 얼마인지 모르겠는데?"에 대한 답으로 등장해야 한다.

**①이 은퇴가 아니어도 되는 이유**: 은퇴는 20대에게 너무 멀어 체감되지 않는다.
목표가 가까울수록 "덜 사면 더 가까워진다"가 작동한다.

---

## 5. 입력 방식

```
링크 붙여넣기
      ↓
리다이렉트 따라가기  ← 필수
      ↓
OG 태그 파싱
      ↓ 실패 시
   수동 입력 폼  ← 폴백 (필수)
      ↓
항목별 "이유" 객관식 선택
```

주요 쇼핑몰 OG 파싱은 Vercel 환경에서 이미 동작 확인됨.
다만 어떤 사이트가 들어올지 모르므로 **폴백은 반드시 구현한다.**

### 리다이렉트를 따라가야 한다

앱에서 "공유"를 누르면 딥링크 단축 URL이 나온다 (예: `musinsa.onelink.me/...`).
**이 URL 자체에는 OG 태그가 없고**, 실제 상품 페이지로 리다이렉트된 뒤에야 나온다.
fetch 시 리다이렉트를 따라가도록 설정하고, 최종 URL을 저장한다.

### 가져올 메타 필드

제품명·가격·이미지 외에 아래도 같이 가져온다. 판정 정확도에 직접 영향을 준다.

| 필드 | 용도 |
|---|---|
| 카테고리 | LLM 추정 정확도 향상. **`cart_duplication` 판정에 직접 사용** — 같은 카테고리 항목이 여러 개면 바로 잡힘 |
| 정가 + 할인율 | `product:price:normal_price`, `sale_rate`. **"세일 중"을 사용자가 고르지 않아도 감지 가능** |
| 브랜드 | 보조 정보 |

할인율이 높은데 사용자가 다른 이유를 골랐다면 그것대로 신호가 된다.

### 실측 예시 (무신사)

```
입력   https://musinsa.onelink.me/PvkC/ddjw8k43
  ↓ 리다이렉트
최종   https://www.musinsa.com/products/6880040

og:title                    아캄(AAKAM) Napoleon Corded Zip-Up Hoodie (Teal) …
og:image                    (있음)
product:price:amount        143100
product:price:normal_price  159000
product:price:sale_rate     10
product:brand               아캄
카테고리                     아우터 > 후드 집업
```

### 이유 객관식

주관식이면 표현이 제각각이라 LLM 점수가 흔들린다. 선택지로 고정한다.

| 선택지 | 신호 |
|---|---|
| 오래전부터 갖고 싶었음 | 지속적 욕구 → 가산 |
| 지금 당장 필요함 | 긴급성 → 가산 |
| 쓰던 게 망가짐/떨어짐 | 대체 필요 → 가산 |
| 세일 중이라서 | 앵커링 신호 → 감산 |
| 남들이 좋다고 해서 | 사회적 증거 신호 → 감산 |
| 그냥 기분전환 | 만족 지속 짧음 → 감산 |

---

## 6. AI 판정 로직

### 6-1. LLM이 추정하는 것 — **확정: 절충안(3개 LLM + 2개 규칙 기반)**

> **Phase 1 실행 결과로 확정됨** (2026-09-18, `scripts/phase1-llm-check.mjs`).
> `temperature` 파라미터가 이 모델(claude-sonnet-5)에서 거부(deprecated)되어 0으로 고정할 수 없었다.
> 3회 반복 중 2회는 순위가 완전히 동일했고, 1회는 점수가 근소한 인접 항목(3~4위)끼리만 순서가 바뀌었다 —
> 전체 구조(1위/꼴찌)는 항상 안정적. temperature를 못 고정하는 상황에서 LLM 추정 필드를 늘리면
> 흔들림만 커지므로, 아래 **절충안**(3개만 LLM에 추정시키고 나머지 2개는 규칙 기반)으로 확정.
> 가격순과 AI순위가 다르다는 것도 같은 실행에서 확인됨 (검증 포인트 통과).

#### 확정된 것

| 필드 | 설명 |
|---|---|
| `satisfaction_months` | 만족이 지속될 예상 개월 수 |

**핵심 변수.** 기회비용을 이것으로 나눠서 가격과 독립적인 지표를 만든다 (6-4 참고).
이것 하나는 반드시 들어간다.

#### 유력 후보

| 필드 | 설명 | 특징 |
|---|---|---|
| `usage_frequency` | 매일 쓸지, 가끔 쓸지 | LLM 추정 정확도 좋음. 만족 지속과 약간 겹침 |
| `cart_duplication` | 장바구니 안에 같은 목적의 항목이 또 있는지 | **전 항목을 한 번에 넣기 때문에 가능한 지표.** 항목별 개별 평가로는 절대 안 나온다. 원래 문제(잔뜩 쌓여서 못 고름)에 직접 대응 |
| `price_volatility` | 기다리면 싸질지, 신모델이 나올지 | **"미루기" 판정에 직접 쓰인다.** 다른 지표는 사라/사지마라만 말하는데 이건 시점을 말해줌 |
| `inconvenience_if_not` | 없어서 겪을 실질적 불편 | 순위를 크게 바꿈. 다만 "필요도"와 개념이 겹침 |

#### 탈락시킨 후보 (이유 포함)

| 필드 | 탈락 이유 |
|---|---|
| `replaceability` (이미 갖고 있을 확률) | **LLM은 사용자가 뭘 갖고 있는지 모른다.** 카테고리 보고 추측하는 수준이라 신뢰도가 낮음 |
| `time_sensitivity` (지금 사야 하는 정도) | 이유 객관식의 "지금 당장 필요함", "세일 중이라서"와 **같은 신호를 두 번 세는 구조** |
| `regret_probability` (후회 확률) | 다른 지표들의 합성값. 따로 재면 중복 계산 |
| `necessity` (필수성, need/want) | 이유 객관식과 중복 |
| `product_lifespan` (제품 수명) | `satisfaction_months`와 크게 겹침 |

#### 3개 안 vs 5개 안

| | 구성 | 장점 | 단점 |
|---|---|---|---|
| **3개** | satisfaction_months + usage_frequency + cart_duplication | LLM 일관성이 안정적. 서로 안 겹침. 정리라는 목적에 밀착 | "언제 사야 하나"에 대한 답이 없음 |
| **5개** | 위 3개 + price_volatility + inconvenience_if_not | 조언이 구체적. "3개월 뒤에 사라" 같은 말이 가능 | **항목마다 5개 값을 추정하면 일관성이 떨어질 위험.** 3개일 때보다 확실히 흔들림 |

**절충안**: LLM은 3개만 추정하고, 나머지 2개는 다른 방식으로 얻는다.
- `price_volatility` → 카테고리 기반 규칙으로 대략 판정 (전자기기·의류는 변동 큼, 소모품은 작음)
- `inconvenience_if_not` → 이유 객관식에서 이미 신호가 나옴

**Phase 1에서 3개 안과 5개 안을 각각 돌려보고 재현성을 비교한 뒤 결정한다.**

> 참고: 사용자가 판단 기준을 직접 고르게 하는 안도 검토했으나 제외했다.
> 기준을 고를 수 있다는 건 자기가 뭘 중요하게 보는지 안다는 뜻인데,
> 그런 사람은 애초에 결정 마비를 겪지 않는다. 결정을 하나 더 얹는 셈이 된다.
> 대신 **결과를 먼저 보여준 뒤** 가중치 슬라이더로 조정하게 한다 (결과를 본 다음이라 판단이 쉬움).

### 6-2. 호출 방식

- **전 항목 한 번에 평가** (항목별 개별 호출 금지 — 비교 기준이 흔들림)
- ~~`temperature: 0`~~ → **claude-sonnet-5는 `temperature` 파라미터를 거부한다** (Phase 1에서 확인, `invalid_request_error`). 보내지 않는다. 대신 프롬프트에 평가 기준을 최대한 명시적으로 고정해서 흔들림을 줄인다 (6-1 참고 — 이 때문에 LLM 추정 필드를 3개로 최소화함)
- 평가 기준을 프롬프트에 명시적으로 고정
- 서버 라우트에서만 호출 (API 키 클라이언트 노출 금지)

**출력 스키마 (필드는 6-1 확정 후 조정)**

```json
{
  "items": [
    {
      "id": "...",
      "satisfaction_months": 24,
      "usage_frequency": 0.8,
      "cart_duplication": 0.0,
      "reasoning": "한 줄 근거"
    }
  ]
}
```

### 6-3. 사용자에게 묻는 것 — 딱 하나

> **"이 중에 하나만 살 수 있다면?"**

순위를 전부 매기게 하면 노동이고, 무엇보다 **순위를 매길 수 있으면 이미 결정한 것**이다.
원래 문제가 그걸 못 하는 것이므로 문제를 사용자에게 되돌려주는 셈이 된다.

하나 고르기는 부담이 적으면서 얻는 정보는 거의 같다.

### 6-4. 점수 계산 (코드에서)

기회비용을 그대로 쓰면 `가격 × 상수`라 가격과 같은 정보가 된다.
예산 제약에서 이미 가격을 쓰므로 순위 계산에 아무것도 추가하지 못한다.
**만족 지속 개월로 나눠서 가격과 독립적인 지표로 만든다.**

```
기회비용        = price × (1 + r)^n
월당 기회비용    = 기회비용 ÷ satisfaction_months
정성점수        = f(reason_code, 그 외 LLM 추정 필드)   ← 6-1 확정 후 결정
최종점수        = 정성점수 ÷ 월당 기회비용        (높을수록 상위)
```

사용자가 고른 1개에는 가중치를 부여한다. 매번 뒤집으면 신뢰가 깨진다.
단, 절대적이진 않게 한다.

**검증 포인트**: 가격순과 순위가 달라야 한다.
똑같이 나오면 `satisfaction_months`가 제 역할을 못 하는 것이므로
프롬프트나 계산식을 손봐야 한다.

### 6-5. 출력 — 추천 N개가 아니라 순위

```
① 노트북          120만원   ─┐
② 러닝화           15만원    │ ← 예산 150만원선
─────────────────────────────┘
③ 헤드폰           25만원      ← "10만원만 더 있으면"
④ 콘서트 티켓      15만원
⑤ 향수              9만원
```

순위 방식인 이유:

- 예산이 바뀌어도 순위는 그대로, 선만 움직임 → 재계산 불필요
- "조금만 더 있으면 뭐까지" 가 한눈에 보임
- 부분 수용 가능 (1등은 동의, 2등은 거부)
- 1·2등을 샀으면 다음 달엔 3등이 1등 → 장바구니가 계속 갱신됨

### 6-6. 선택 갭 설명

사용자 선택과 AI 상위 항목이 어긋날 때가 가장 가치 있는 순간이다.

> "당신은 A를 골랐는데 계산상으로는 B가 앞서요.
>  A는 만족이 2주 정도인데 B는 2년 가거든요.
>  그래도 A가 맞다면 그건 그것대로 괜찮아요."

마지막 줄이 중요하다. 판정이 아니라 관점 제공이다.

---

## 7. 예산 산출 (선택 경로)

수입·소비 입력 → 몬테카를로 1,000회 → 목표 달성 시점 분포 → 역산한 적정 월 예산

### 변수는 제한한다

| 넣는 것 | 빼는 것 |
|---|---|
| 물가상승률 | 산업 구조 변화 |
| 수익률 변동성 (분포) | 정책 변화 |
| 소득 증가율 | 그 밖의 거시 시나리오 |

하루 안에 모델링할 수도, 근거를 댈 수도 없는 변수는 넣지 않는다.
**가정값은 화면에 노출하고 사용자가 조정 가능하게 한다.**

---

## 8. 게임성

> **비주얼 명세는 `DESIGN.md` 에 분리했다.**
> 색 팔레트, 타이포, 형태, 에셋 방침은 그쪽을 따른다.
> 디자인 작업은 별도 도구(Stitch)로 진행할 예정이므로 **임의로 스타일을 정하지 않는다.**
> 구현 중에는 최소한의 임시 스타일만 쓰고, 색은 나중에 한 번에 바꿀 수 있게 변수로 뺀다.

여기에는 **구현에 직접 관련된 것만** 적는다.

### 8-1. 연출이 붙는 지점

전부 움직일 필요는 없다. 아래 네 곳에 집중한다.
**기능과 연출이 한 몸이므로 Phase 3~4에서 같이 만든다.**

| 지점 | 동작 |
|---|---|
| 항목 옮기기 | 집어서 옮기는 느낌, 놓을 때 반동 |
| **내리기** (실제로 삼) | 변기물 내리듯 시원하게 사라짐 |
| **빼기** (안 사기로 함) | 목록에서 확 털어냄 |
| **순위 공개** | 위에서부터 순차적으로 드러냄 |

가운데 둘이 이 서비스의 쾌감을 만든다.

### 8-2. 시그니처 캐릭터

**한 캐릭터가 두 역할을 겸한다.** 생김새는 `DESIGN.md` 6-5 참고.

**① 진행률 러너**
- 화면 하단 고정, 스크롤해도 계속 보임
- 목표까지의 진행률 + 달려가는 모습
- 목표는 사용자가 ①에서 고른 것
- **후퇴 연출 없음** — 예산 안에서 산 것은 정상 소비다 (원칙 ②).
  대신 전진 폭을 다르게 해서 변화를 만든다

**② AI 화자**
- 순위 결과, 예산 안내, 선택 갭 설명을 이 캐릭터가 말하는 형태로 출력
- 로딩 상태도 캐릭터의 "생각 중" 표현으로 대체
- "AI가 분석했습니다" 같은 기계적 문구를 쓰지 않아도 된다

**말투는 잔소리가 되면 안 된다.** 같이 목표를 향해 달리는 동료에 가깝게.
판정관이나 감시자가 아니다 (원칙 ②③). LLM 프롬프트에 이 톤을 명시할 것.

### 8-3. 사운드

여유가 있으면 넣는다. **자동 재생 금지, 음소거 토글 필수.**

## 9. 기술 스택

### 9-0. 데스크톱 · 모바일 양쪽 대응

**둘 다 제대로 작동해야 한다.** 한쪽을 곁다리로 두지 않는다.

- **모바일**: 링크 붙여넣기도 쇼핑도 원래 폰에서 하는 동작이고,
  실사용자에게 링크를 돌리면 상당수가 폰으로 연다
- **데스크톱**: 항목이 여러 개 쌓인 걸 한눈에 보고 정리하는 작업이라
  넓은 화면이 실제로 유리하다. 심사·데모도 데스크톱일 가능성이 높다

두 화면에서 **2단 구조를 보여주는 방식이 달라진다.** 여기가 핵심 분기점이다.

| | 장바구니 / 살 물건 배치 | 항목 옮기기 | 진행률 캐릭터 |
|---|---|---|---|
| 데스크톱 | 좌우 분할 | 드래그 앤 드롭 | 하단 고정 바 |
| 모바일 | **위아래 분할** | **길게 눌러 드래그** | 하단 고정, 높이 최소화 |

양쪽 모두 "두 영역을 동시에 보면서 옮긴다"는 같은 감각을 유지한다.
데스크톱의 좌우 분할은 그냥 넓어서가 아니라 **"옮긴다"는 동작이 시각적으로 성립**하기 때문이고,
모바일의 위아래 분할도 같은 이유다.

**단, 드래그는 기본 동작이 아니라 추가 동작으로 둔다.**

```
기본   항목 탭해서 선택 → "옮기기" 버튼
추가   길게 눌러 드래그
```

터치 드래그는 두 스크롤 영역 사이를 오갈 때 자동 스크롤까지 처리해야 해서
구현 비용이 생각보다 크다. 기본 경로를 먼저 만들고 드래그를 얹으면,
시간이 부족할 때 드래그만 빠지고 기능은 온전히 남는다.

모바일에서는 분할된 각 영역이 짧아지므로 내부 스크롤이 필요하다.
항목이 많을 때 어떻게 보일지 확인할 것.

화면별 구성과 시각 지침은 `DESIGN.md` 6번 참고.

공통:
- 터치 대상은 충분히 크게 (데스크톱에서도 손해 없음)
- 진행률 캐릭터는 스크롤해도 계속 보이게
- 연출·애니메이션은 양쪽에서 모두 확인할 것

### 9-1. 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | |
| 배포 | Vercel | |
| DB | Supabase 또는 Vercel Postgres | UUID 링크 저장용 |
| LLM | Anthropic API | 서버 라우트 전용 |
| 몬테카를로 | **JS로 구현** | Python 백엔드 두면 배포 대상이 둘이 됨 |

### 9-2. 실패 처리

데모 도중에 터지는 게 가장 아프다. 아래는 **반드시** 처리한다.

| 상황 | 처리 |
|---|---|
| LLM이 JSON 형식을 깨뜨림 | 파싱 실패 시 1회 재시도 → 그래도 실패하면 아래 폴백 |
| LLM 응답에 항목이 누락됨 | 누락된 항목만 기본값으로 채우고 나머지는 정상 표시 |
| API 호출 실패 / 타임아웃 | **가격순 정렬로라도 순위를 보여준다.** 빈 화면은 금지 |
| OG 파싱 실패 | 수동 입력 폼으로 전환 (5번 참고) |
| DB 연결 실패 | 로컬 상태로라도 동작시키고 "저장 안 됨" 표시 |

공통 원칙: **어떤 실패든 화면이 비지 않게 한다.**
에러 메시지만 띄우고 끝내지 말고, 할 수 있는 만큼이라도 보여준다.

LLM 호출 중에는 로딩 상태를 보여준다. 몇 초 걸리므로 아무 반응이 없으면
사용자가 버튼을 다시 누르게 되고, 그만큼 API 비용도 늘어난다.
**호출 중 버튼 비활성화는 필수.**

### 9-3. 데이터 모델

```
sessions
  id (uuid, PK)
  goal_type        목표 종류
  goal_amount      목표 금액
  monthly_budget   월 예산
  created_at

items
  id
  session_id (FK)
  name
  price
  image_url
  reason_code      객관식 선택값
  status           'cart' | 'buy' | 'removed' | 'purchased'
  created_at

evaluations
  item_id (FK)
  satisfaction_months
  (그 외 LLM 추정 필드 — 6-1 확정 후 결정)
  score
  reasoning
  evaluated_at
```

`status` 4종으로 2단 구조 + 빼기/내리기가 전부 표현된다.

---

## 10. 구현 순서

시간은 대략적인 흐름이지 마감이 아니다. **순서와 우선순위가 핵심.**

| Phase | 내용 | 대략 |
|---|---|---|
| 0 | 셋업, 빈 페이지로 배포 1회, API spend limit 설정, DB 스키마 | ~45분 |
| 1 | **LLM 파이프라인 단독 확인** (UI 없이 스크립트로) | ~2시간 |
| 2 | 링크 파싱 + 항목 등록 + 이유 선택 | ~1.5시간 |
| 3 | 2단 구조 + 빼기/내리기 **+ 이 동작들의 연출** | ~2시간 |
| 4 | 목표·예산 + AI 판정 연결 + 순위 출력 **+ 공개 연출** | ~2시간 |
| 5 | **1차 배포 + 사람 투입** | ~1시간 |
| 6 | 진행률 캐릭터, 몬테카를로 예산 산출 | |
| 7 | 피드백 반영, 디자인 적용, 데모 준비 | |

### 비주얼은 어떻게 처리하는가

**디자인은 별도 도구(Stitch)로 사용자가 진행한다.** 스타일을 임의로 만들지 않는다.

- 구현 중에는 최소한의 임시 스타일만 쓴다. 꾸미지 않는다 — 어차피 교체된다
- 색이 필요하면 `DESIGN.md` 의 참고값을 쓰되, 그건 확정된 팔레트가 아니다.
  나중에 한 번에 바꿀 수 있도록 **색을 하드코딩하지 말고 변수로 뺀다**
- 다만 **연출(8-1)은 기능과 한 몸**이라 Phase 3~4에서 같이 만든다.
  나중에 붙이면 두 번 일하게 된다
- Phase 7에서 완성된 디자인을 적용한다

### Phase 1을 먼저 하는 이유

UI는 나중에 고쳐도 되지만, LLM 순위가 엉망이면 서비스가 통째로 무의미해진다.
여기가 제일 불확실하므로 먼저 확인한다. 시드 데이터 6개 하드코딩으로 충분하다.

확인할 것:
- 같은 입력 3회 → 순위 동일한가 (재현성)
- 순위를 보고 납득되는가
- **가격순과 순위가 다른가**

### Phase 3이 끝나면

**AI 없이도 쓸 수 있는 도구**가 된다. 이 상태에서 이미 가치가 성립하는 게 중요하다.

### 뒤처질 때 줄이는 순서

```
먼저 줄임    몬테카를로 예산 산출 → 예산 직접 입력만 남김
그 다음      선택 갭 설명
그 다음      사운드
마지막       진행률 캐릭터
```

가능하면 남길 것: **2단 구조, 빼기/내리기(+연출), 순위 + 예산선.** 이 셋이 골격이다.

빼기/내리기 연출은 기능과 한 몸이므로 같이 남긴다.
여기가 이 서비스의 재미가 가장 크게 걸린 지점이다.

---

## 11. 하지 말 것

| 함정 | 대응 |
|---|---|
| LLM에게 순위를 직접 물음 | 파라미터만 추정, 순위는 코드 계산 |
| 항목별로 개별 LLM 호출 | 전 항목 일괄 1회 |
| 임의로 스타일을 만들어 붙임 | 디자인은 Stitch로 별도 진행. 임시 스타일만 |
| 색을 하드코딩함 | 나중에 교체되므로 변수로 뺄 것 |
| 연출을 마지막에 몰아서 함 | 기능과 한 몸. Phase 3~4에서 병행 |
| 배포를 마지막에 함 | Phase 5에 한 번 올림 |
| API 키를 클라이언트에 노출 | 서버 라우트에서만 호출 |
| spend limit 설정 잊음 | Phase 0에서 처리 |
| 가격순과 순위가 똑같이 나옴 | Phase 1에서 확인 |
| 몬테카를로를 Python으로 | JS로 구현 |
| "사도 됨"을 경고처럼 표시 | 원칙 ② |
| 죄책감 유발 문구 | 원칙 ③ |
| 단축 딥링크에서 OG 파싱 실패 | 리다이렉트를 따라가야 함 (5번 참고) |
| 한쪽 화면만 확인하고 넘어감 | 9-0. 데스크톱·모바일 둘 다 작동해야 함 |
| 실패 시 빈 화면 | 9-2. 할 수 있는 만큼이라도 보여준다 |
| LLM 호출 중 버튼이 계속 눌림 | 로딩 상태 + 버튼 비활성화 |

### 하루 스코프에서 넣지 않을 것

아래는 평소엔 좋은 습관이지만 이 프로젝트에선 시간을 잡아먹는다.
요청받지 않으면 하지 않는다.

- 테스트 코드
- 접근성 대응
- 다국어(i18n)
- 상세 로깅 / 모니터링
- 성능 최적화
- 과도한 컴포넌트 추상화

---

## 12. 표시해야 할 고지

- 만족 지속 개월·대체 가능성은 LLM 추정값이며 실측이 아님
- 참고용 판단이며 투자·재무 조언이 아님
- 지출 결정의 책임은 사용자에게 있음

---

## 13. 아직 안 정한 것 — 진행 전 확인 필요

wayfinder 맵([github.com/yejooi/bypp#1](https://github.com/yejooi/bypp/issues/1))에서 결정 완료:

| 항목 | 결정 |
|---|---|
| LLM 추정 지표 3개 vs 5개 | ✅ 절충안(3개 LLM + 2개 규칙 기반) — 6-1 참고, Phase 1 실행으로 확정 |
| 목표 선택지 목록 | ✅ 하이브리드: 고정 3개(1억 모으기/내집마련/여행 자금) + 기타 직접입력 — [#3](https://github.com/yejooi/bypp/issues/3) |
| 수익률 · 물가상승률 기본값 | ✅ 수익률 평균 7%/표준편차 9%, 물가상승률 2% — [#4](https://github.com/yejooi/bypp/issues/4), `docs/research/return-inflation-defaults.md` |
| 정성/정량 가중치 초기값 | ✅ 슬라이더 30~100 (완전 가격순 제외), 기본값 65 — [#5](https://github.com/yejooi/bypp/issues/5) |
| 예산선 아래 항목 안내 문구 | ✅ 시나리오별 문구 확정 — [#6](https://github.com/yejooi/bypp/issues/6) |

아직 열려 있는 것:

| 항목 | 상태 |
|---|---|
| 서비스명 | Flushlist / Unstuck 2개로 압축, 개발 착수 후 최종 결정 — [#2](https://github.com/yejooi/bypp/issues/2) |
| 데모용 시드 데이터 | 체크리스트 전달됨, 사용자가 실제 상품으로 준비 중 — [#7](https://github.com/yejooi/bypp/issues/7) |

이 항목들은 임의로 정하지 말고 물어볼 것.

---

## Agent skills

### Issue tracker

Issues live as GitHub issues in this repo (github.com/yejooi/bypp), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
