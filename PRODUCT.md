# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

돈은 모으고 싶은데 사고 싶은 것도 많아서, 장바구니에 잔뜩 쌓아두고 결국 아무것도 못 정하는 사람. 주로 20대이고, 링크를 폰에서 공유해 붙이고 데스크톱에서 목록을 한눈에 정리하는 식으로 두 화면을 다 쓴다. 무신사, 지그재그, 쿠팡, 네이버쇼핑 같은 여러 몰의 장바구니를 동시에 들고 있다.

## Product Purpose

과소비를 막는 도구가 아니라 결정 마비를 끝내주는 도구. 성공은 사용자가 미뤄둔 구매 결정이 실제로 정리되는 것이고, "사도 됨" 판정도 성공으로 친다. 목록이 줄어드는 것(빼기/내리기)이 핵심 가치다. 현재 단계는 1인 개발 해커톤(BYPP) 출품작의 데모와 소수 실사용자 피드백 수집.

## Positioning

기존 위시리스트/가계부 앱은 "너무 많이 산다"를 전제로 안 사게 막는다. 이 서비스는 "못 고른다"를 전제로, 예산 안에서 순위를 매기고 결정을 끝내준다. AI는 항목별 파라미터(만족 지속 개월, 사용 빈도, 장바구니 내 중복)만 추정하고 순위 계산은 코드가 하며, 최종 순서는 사용자가 직접 정한다.

## Operating Context

- 흐름: 로그인(닉네임+비밀번호) → 목표 선택 → 월 예산 설정(직접 입력 또는 몬테카를로 역산) → 링크나 스크린샷으로 아이템 등록 → 아이템 주머니에서 판정대로 후보를 옮겨 AI 판정 → 쇼케이스에서 순서 정렬, 예산선 아래는 자동으로 2층 → 구매(내리기)/빼기.
- 상품 정보는 링크의 OG 태그를 서버에서 파싱하고, 실패하면 수동 입력. 장바구니 자체는 로그인/JS 렌더링 때문에 스크래핑 불가라 스크린샷 인식을 대안으로 쓴다.
- 다른 사용자의 위시리스트를 읽기 전용으로 구경할 수 있다.
- 배포는 Vercel, 데이터는 Supabase, LLM은 Anthropic API(서버 라우트 전용).

## Capabilities and Constraints

- 화폐는 원(KRW), 언어는 한국어.
- 데스크톱과 모바일 둘 다 제대로 동작해야 한다. 드래그는 마우스 즉시, 터치는 길게 눌러 시작.
- 어떤 실패든 화면이 비지 않게 한다 (LLM JSON 재시도, 누락 항목 기본값, DB 실패 시 로컬 동작).
- 하루 스코프 제외 항목: 테스트 코드, 접근성 대응, i18n, 상세 로깅, 성능 최적화.
- 미결정: 서비스명(Flushlist / Unstuck 후보), 데모용 실제 상품 시드 데이터.

## Brand Commitments

서비스명은 미정 (Flushlist / Unstuck 후보). 메시지는 해결책 형태로 쓰고 죄책감이나 손실 프레임을 쓰지 않는다. 화면 속 안내자는 판정관이 아니라 같이 달리는 동료의 톤.

## Evidence on Hand

- 실제 후기, 고객, 지표는 없다. 만들어내지 말 것.
- 검증된 것: 무신사 상품 페이지 OG 파싱, 가격순과 AI 순위가 다르게 나오는 것, 카테고리 중복 감지.
- Stitch 디자인 시안이 `stitch_custom_ui_design_system*/`에 있다.

## Product Principles

1. 결정권은 사용자에게 있다. AI는 막혔을 때만 등장하고, 최종 순서는 사용자가 정한다.
2. 절약을 강요하지 않는다. "사도 됨"은 실패가 아니고 예산 안의 구매는 정상 소비다.
3. 메시지는 해결책 형태로 쓴다 ("2개 빼면 이번 달 안에 들어와요").
4. LLM에게 순위를 직접 묻지 않는다. 추정은 LLM, 계산은 코드.
5. 재미는 장식이 아니라 기능이다. 쓸모 있어서가 아니라 쓰고 싶어서 쓰게 만든다.

## Accessibility & Inclusion

제품 고유의 요구사항은 아직 정해지지 않았다.
